{{ form_start(form) }}
{{ form_row(form.leadSource, {
    'attr': { 
        'data-hx-target': "#lead-source-form-" ~ page.id,
        'data-hx-post': path('settings.facebook.page.lead_source', {'id': page.id })
        }
    }) }}
{{ form_end(form) }}
