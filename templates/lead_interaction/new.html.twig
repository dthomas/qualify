{% extends 'base.html.twig' %}

{% block title %}Lead Interaction
{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-content">
            <p class="card-header">
                <div class="is-flex is-justify-content-space-between is-align-items-center">
                    <p class="title is-5 my-0">{{ lead.name }}</p>
                    <p>
                        <a class="button is-small is-link is-light is-rounded" href="{{ path('lead.list') }}">Back</a>
                    </p>
                </div>
            </p>
        </div>
        <div class="card-content" id="lead-workspace">
            {% include "lead_interaction/_lead.html.twig" %}
        </div>
    </div>
{% endblock %}
