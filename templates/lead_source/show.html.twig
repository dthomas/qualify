{% extends 'base.html.twig' %}

{% block title %}LeadSource{% endblock %}

{% block body %}
    <h1>LeadSource</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lead_source.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ lead_source.code }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ lead_source.name }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ lead_source.isActive ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ lead_source.createdAt ? lead_source.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ lead_source.updatedAt ? lead_source.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lead_source_index') }}">back to list</a>

    <a href="{{ path('lead_source_edit', {'id': lead_source.id}) }}">edit</a>

    {{ include('lead_source/_delete_form.html.twig') }}
{% endblock %}
