<form method="post" action="{{ path('lead_stage.delete', {'id': lead_stage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lead_stage.id) }}">
    <button class="btn">Delete</button>
</form>
